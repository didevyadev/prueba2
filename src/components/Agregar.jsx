import React, { Component } from 'react'

export default class Agregar extends Component{
    render(){
        const { prueba } = this.props
        return(
        <h5>{prueba}</h5>
        )
    }
}